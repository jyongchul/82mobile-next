---
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - components/ui/Button.tsx
  - components/shop/ProductCard.tsx
  - components/cart/CartDrawer.tsx
  - components/checkout/CheckoutForm.tsx
autonomous: true
---

# Plan 05-03: Touch Target Optimization

## Objective

Ensure all interactive elements meet WCAG 2.1 AA minimum touch target size (44x44px) on mobile devices for improved usability and accessibility.

## Context

Success criteria requires all buttons meet 44x44px minimum tap size. Many current buttons may be smaller than this on mobile viewports, causing tap accuracy issues. Need to audit and fix all interactive elements.

## Tasks

<task id="1" name="Audit current touch targets">
Identify undersized touch targets:
- Scan all Button components
- Check ProductCard interactive elements
- Review CartDrawer buttons
- Check CheckoutForm inputs and buttons
- Test header navigation items
- List all elements <44x44px on mobile
</task>

<task id="2" name="Update Button component">
Modify `components/ui/Button.tsx`:
- Add mobile-specific sizing classes
- Ensure min-height: 44px on mobile
- Add padding for comfortable tap area
- Increase spacing between adjacent buttons
- Maintain visual hierarchy
- Test all button variants (primary, secondary, ghost)
</task>

<task id="3" name="Fix ProductCard interactions">
Update `components/shop/ProductCard.tsx`:
- "Add to Cart" button: min 44x44px
- Card click area: ensure adequate size
- Product expansion button: increase size
- Quantity controls: +/- buttons 44x44px
- Test on various mobile sizes
</task>

<task id="4" name="Update CartDrawer controls">
Modify `components/cart/CartDrawer.tsx`:
- Quantity controls (+/-): 44x44px minimum
- Remove item button: increase size
- Close drawer button: ensure 44x44px
- "Proceed to Checkout": adequate size
- Spacing between controls: min 8px
</task>

<task id="5" name="Optimize form inputs">
Update `components/checkout/CheckoutForm.tsx`:
- Input fields: min-height 44px
- Radio buttons: increase clickable area
- Checkboxes: expand touch target
- Submit button: prominent size
- Error state buttons: maintain size
</task>

<task id="6" name="Navigation touch targets">
Update header navigation:
- Menu toggle: 44x44px
- Cart icon: increase size
- Language switcher: adequate tap area
- Logo: clickable area sufficient
- Mobile menu items: 44px height
</task>

<task id="7" name="Responsive spacing">
Adjust spacing for mobile:
- Buttons stacked vertically: 12px gap
- Buttons side-by-side: 8px gap minimum
- Form fields: 16px vertical spacing
- Section padding: 16px mobile, 24px desktop
</task>

## Verification

**Manual Testing:**
- [ ] Test all buttons on 320px viewport (iPhone SE)
- [ ] Test on 375px viewport (iPhone 12/13)
- [ ] Test on 414px viewport (iPhone 12 Pro Max)
- [ ] Measure touch targets with Chrome DevTools
- [ ] Verify spacing between adjacent buttons
- [ ] Test with large fingers (thumb navigation)
- [ ] Check landscape orientation

**Accessibility:**
- [ ] WCAG 2.1 Level AA compliance
- [ ] All interactive elements ≥44x44px
- [ ] Adequate spacing between targets
- [ ] Focus indicators visible
- [ ] Screen reader announces buttons correctly

**Code Quality:**
- [ ] Tailwind responsive classes used correctly
- [ ] No layout shift between viewports
- [ ] TypeScript types maintained
- [ ] Component variants work on mobile

## Must-Haves

1. **All buttons ≥44x44px**: Every interactive element meets minimum size
2. **Adequate spacing**: Minimum 8px between adjacent touch targets
3. **Mobile-optimized forms**: Form inputs and controls properly sized
4. **Navigation accessible**: Header and menu items easy to tap

## Dependencies

- Existing UI components (Button, ProductCard, CartDrawer)
- Tailwind CSS responsive utilities
- WCAG 2.1 AA guidelines

## Success Metrics

- 100% of interactive elements ≥44x44px on mobile
- Lighthouse Accessibility score >90
- Manual testing: no mis-taps
- User testing: improved tap accuracy
